import { FC } from "react";

interface BaseComponentProps {}

const BaseComponent: FC<BaseComponentProps> = () => {
  return <div>This is a template for creating components.</div>;
};

export default BaseComponent;

// Use this page to quickly structure your own components by copying and pasting it to a new .tsx file.
// Find+Replace "BaseComponent" with "YourComponentName"
