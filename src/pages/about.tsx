//Delete this but you can use it as a general template for creating pages.

import type { NextPage } from "next";

const About: NextPage = () => {
  return (
    <div className="mx-auto max-w-4xl">
      <h1 className="mt-6 flex text-2xl">/about</h1>
      <p className="mt-4">
        too lazy to write rn ill add this some other time 😀
      </p>
    </div>
  );
};

export default About;
